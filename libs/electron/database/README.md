# database-electron

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build database-electron` to build the library.

## Running unit tests

Run `nx test database-electron` to execute the unit tests via [Jest](https://jestjs.io).
