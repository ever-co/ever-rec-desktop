# window

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build window` to build the library.

## Running unit tests

Run `nx test window` to execute the unit tests via [Jest](https://jestjs.io).
