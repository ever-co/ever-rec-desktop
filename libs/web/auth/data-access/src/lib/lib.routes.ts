import { Route } from '@angular/router';
import { AuthDataAccessComponent } from './auth-data-access/auth-data-access.component';

export const authDataAccessRoutes: Route[] = [
  { path: '', component: AuthDataAccessComponent },
];
