import { createActionGroup, emptyProps, props } from '@ngrx/store';

export const TimelineActions = createActionGroup({
  source: 'Timeline',
  events: {
    'Timeline Timelines': emptyProps(),
    
    
  }
});
