{
  "name": "screenshot-data-access",
  "$schema": "../../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/web/screenshot/data-access/src",
  "prefix": "lib",
  "projectType": "library",
  "tags": [],
  "targets": {
    "build": {
      "executor": "@nx/angular:package",
      "outputs": ["{workspaceRoot}/dist/{projectRoot}"],
      "options": {
        "project": "libs/web/screenshot/data-access/ng-package.json"
      },
      "configurations": {
        "production": {
          "tsConfig": "libs/web/screenshot/data-access/tsconfig.lib.prod.json"
        },
        "development": {
          "tsConfig": "libs/web/screenshot/data-access/tsconfig.lib.json"
        }
      },
      "defaultConfiguration": "production"
    },
    "test": {
      "executor": "@nx/jest:jest",
      "outputs": ["{workspaceRoot}/coverage/{projectRoot}"],
      "options": {
        "jestConfig": "libs/web/screenshot/data-access/jest.config.ts"
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint"
    },
    "version": {
      "executor": "@jscutlery/semver:version",
      "options": {
       "syncVersions": false,
        "push": true,
        "pushTags": true,
        "trackDeps": true
      }
    },
    "publish": {
      "executor": "nx:run-commands",
      "options": {
        "parallel": false,
        "commands": [
          "nx version screenshot-data-access --releaseType=patch --pushTags",
          "nx build screenshot-data-access",
          "cd dist/libs/web/screenshot/data-access && npm publish --access public"
        ]
      }
    }
  }
}
